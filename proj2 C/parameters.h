

// This file is just to link the gloabal variables between files

// Specifies number of slaves (must be > 0)
int NUM_SLAVES;
// Specifies max task length (in seconds)
int MAX_TASK_LEN;
// Specifies max idle time between generating tasks of master thread (in seconds)
int MAX_MASTER_IDLE;
// Specifies max running time of program (0 = no max) in milliseconds
int TIMEOUT;

// next task id
int nextId;
